reserved-names-translation:
  - date: date_
  - timestamp: timestamp_

aliases:
  - snowflake: sf
    duckdb: ddb

servers:
  server-name-pattern: "{alias}-{server}-{databaseName}db-{schemaName}-{environment}"
  id-pattern: "{alias}|{server}|{databaseName}db|{schemaName}|{environment}"
